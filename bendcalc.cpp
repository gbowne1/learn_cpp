#include <iostream>
#include <string>
#include <cmath>
using namespace std;

void get_radius (float&);
float surface_area (float);

cout << fixed << setprecision (4);
cout << " Please enter the bend angle in degree = " << "\n" << "or in radian = " ;

int choice = 0;
cin >> choice;
cout << "Enter the angle: ";
cin >> angle;

cin >> degree << radian;

rad1 = degree * PI / 180; 

switch(angle) 
{
    case"radian": if (radian >= 0 && radian <= 2*PI)
    sin1 = sin(radian);
    cos1 = cos(radian);
    tan1 = tan(radian);
        break;
    case 2: // they want degrees
    case"radian": if (radian >= 0 && radian <= 2*PI)
    sin1 = sin(radian);
    cos1 = cos(radian);
    tan1 = tan(radian);
        break;
        default: // They entered something else
                 // Send some error
}
cout << " Please enter the material thickness in inches = " << "\n" << "or in millimeters = " ;
cin >> size_t (Hidden)
cout << " Please enter the length of side A flange length = " << ; 

cout << " This bend requires x = {value} tonnage " 